# M8 AI 对话功能 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 在 FNS Sync 插件里加"高亮 → 问 AI → 多轮对话 → 加到笔记"功能，Kindle 直连 DeepSeek（兼容 OpenAI API），AI 内容作为独立 AI@ 块通过现有 FNS 同步推送到 Obsidian。

**架构：** 方案 A（Kindle 直连 AI API）。AI 配置全部塞进 `G_reader_settings["fns_sync"]`（**取消独立 `ai_config.lua` 文件**——这是 2026-08-12 回滚后调研发现的 require 缓存坑）。HTTP 调用完全沿用现有 `api.lua` 同步模式（luasocket + rapidjson + `UIManager:nextTick`）。链式对话框用 KOReader 标准 `InputDialog + TextViewer + close A → show B` 模式。AI@ 块格式跟现有 HL@ 块同构，diff 不参与（跟 USER 一样原样保留）。

**技术栈：** Lua 5.1 / KOReader WidgetContainer / luasocket + rapidjson / DeepSeek `POST /chat/completions`。

**前置文档（必读）：**
- 设计规格：`docs/superpowers/specs/2026-08-11-ai-chat-design.md`
- 调研报告：`docs/superpowers/research/2026-08-12-koreader-m8-research.md`
- 回滚复盘：`progress/2026-08-12 daily progress.md`

**关键设计修正**（来自调研报告 §5，对照设计规格的 12 处错误假设）：

1. **高亮菜单加按钮**：用 `self.ui.highlight:addToHighlightDialog(idx, fn_button)` 注册，**不要** override `onShowHighlightMenu`（KOReader 不广播此事件）
2. **AI 配置存储**：塞进 `G_reader_settings["fns_sync"]`，**不要** 新建独立 `ai_config.lua`（require 缓存会导致用户改文件不生效）
3. **配置入口**：菜单输入（`_editString` 模式），**不要** USB 编辑文件入口
4. **HTTP 客户端**：完全沿用 `api.lua` 同步模式，**不要** 引入 Trapper/Spore/httpclient
5. **Timeout**：用 `socketutil:set_timeout(10, 60)`（block 10s / total 60s，给 DeepSeek 30s 推理 + 网络往返留余量）

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `plugin/fns_sync.koplugin/config.lua` | 修改 | `Config.DEFAULTS` 加 AI 字段；`CURRENT_CONFIG_VERSION` 4→5；加 AI 相关常量 |
| `plugin/fns_sync.koplugin/main.lua` | 修改 | init() 加 v4→v5 迁移块；init() 注册 `addToHighlightDialog`；加 AI 子菜单树；加 `_openAiInputDialog` / `_openAiResponseViewer` / `_aiSession` 等方法 |
| `plugin/fns_sync.koplugin/ai.lua` | 新建 | DeepSeek HTTP 调用层（`Ai:chat`），结构对齐 `api.lua`。**不写 UI** |
| `plugin/fns_sync.koplugin/marker.lua` | 修改 | parse / serialize 支持 `type = "ai"` segment；diff 不参与（跟 USER 同构） |
| `tests/test_config_ai.lua` | 新建 | 验证 `Config.DEFAULTS` 包含全部 AI 字段 + 默认值正确 |
| `tests/test_marker_ai.lua` | 新建 | 验证 AI@ 块的 parse / serialize round-trip + diff 不参与 |

**不创建的文件**（设计修正）：

- ❌ `plugin/fns_sync.koplugin/ai_config.lua`（require 缓存坑）
- ❌ `plugin/fns_sync.koplugin/ai_config_override.lua.example`
- ❌ `plugin/fns_sync.koplugin/ai_dialog.lua`（UI 放 main.lua，避免文件爆炸）

---

## 任务依赖图

```
Task A (配置基础设施，TDD)
   ↓
Task B (HTTP 调用层 ai.lua，Kindle 实测)
   ↓
Task C (高亮菜单按钮注册，Kindle 实测)
   ↓
Task D (链式对话框 UI，Kindle 实测)
   ↓
Task E (AI@ 块 marker.lua 扩展，TDD)
```

**强制顺序**：A → B → C → D → E，每步独立 commit + Kindle 实测通过再进下一步。**禁止**堆叠修复（这是 2026-08-12 回滚的直接原因）。

---

## 任务 A：配置基础设施

**目标**：把 AI 配置塞进 `G_reader_settings["fns_sync"]`，为后续 UI 和 HTTP 提供数据基础。

**文件：**
- 修改：`plugin/fns_sync.koplugin/config.lua:18`（版本号）、`:107-189`（DEFAULTS）、末尾追加 AI 常量
- 修改：`plugin/fns_sync.koplugin/main.lua:95-135`（init() 迁移块）
- 创建：`tests/test_config_ai.lua`

### 步骤

- [ ] **A1：编写失败测试**

创建 `tests/test_config_ai.lua`：

```lua
--[[--
Unit tests for M8 AI config defaults.

Run locally (NOT on Kindle) with:
    lua tests/test_config_ai.lua
--]]--

package.path = package.path .. ";./plugin/fns_sync.koplugin/?.lua"

local Config = require("config")

local tests_passed = 0
local tests_failed = 0

local function check(name, cond)
    if cond then
        tests_passed = tests_passed + 1
        print(("  ✓ %s"):format(name))
    else
        tests_failed = tests_failed + 1
        print(("  ✗ %s FAILED"):format(name))
    end
end

print("== M8 AI config defaults ==")

-- Schema version bumped
check("CURRENT_CONFIG_VERSION == 5", Config.CURRENT_CONFIG_VERSION == 5)

-- All AI fields exist in DEFAULTS with correct defaults
check("DEFAULTS.ai_enabled == false", Config.DEFAULTS.ai_enabled == false)
check("DEFAULTS.ai_api_base exists", Config.DEFAULTS.ai_api_base ~= nil)
check("DEFAULTS.ai_api_key == '' (empty by default)", Config.DEFAULTS.ai_api_key == "")
check("DEFAULTS.ai_model exists", Config.DEFAULTS.ai_model ~= nil)
check("DEFAULTS.ai_system_prompt exists", Config.DEFAULTS.ai_system_prompt ~= nil)
check("DEFAULTS.ai_max_tokens == 1024", Config.DEFAULTS.ai_max_tokens == 1024)
check("DEFAULTS.ai_temperature == 0.7", Config.DEFAULTS.ai_temperature == 0.7)
check("DEFAULTS.ai_timeout_sec == 30", Config.DEFAULTS.ai_timeout_sec == 30)

-- Quick prompt templates
check("DEFAULTS.ai_quick_prompts is table", type(Config.DEFAULTS.ai_quick_prompts) == "table")
check("DEFAULTS.ai_quick_prompts.translate exists", Config.DEFAULTS.ai_quick_prompts.translate ~= nil)
check("DEFAULTS.ai_quick_prompts.explain exists", Config.DEFAULTS.ai_quick_prompts.explain ~= nil)
check("DEFAULTS.ai_quick_prompts.comment exists", Config.DEFAULTS.ai_quick_prompts.comment ~= nil)

-- HTTP timeout constants for AI calls
check("Config.AI_HTTP_TIMEOUTS is table", type(Config.AI_HTTP_TIMEOUTS) == "table")
check("Config.AI_HTTP_TIMEOUTS[1] == 10 (block)", Config.AI_HTTP_TIMEOUTS[1] == 10)
check("Config.AI_HTTP_TIMEOUTS[2] == 60 (total)", Config.AI_HTTP_TIMEOUTS[2] == 60)

print(("== Tests: %d passed, %d failed =="):format(tests_passed, tests_failed))
os.exit(tests_failed == 0 and 0 or 1)
```

- [ ] **A2：运行测试验证失败**

运行：

```bash
lua tests/test_config_ai.lua
```

预期：FAIL。报错类似 `CURRENT_CONFIG_VERSION == 5` 失败（当前是 4），`DEFAULTS.ai_enabled` 是 nil 等。

- [ ] **A3：修改 config.lua 升版本号**

在 `plugin/fns_sync.koplugin/config.lua:18` 把：

```lua
Config.CURRENT_CONFIG_VERSION = 4
```

改为：

```lua
Config.CURRENT_CONFIG_VERSION = 5
```

- [ ] **A4：修改 config.lua 在 DEFAULTS 末尾追加 AI 字段**

在 `plugin/fns_sync.koplugin/config.lua` 的 `Config.DEFAULTS = { ... }` 内部，紧接 `offline_queue_enabled = true,` 之后（约第 188 行），追加 AI 字段块：

```lua
    -- M8: AI assistant. All AI config lives here (NOT in a separate
    -- ai_config.lua file — that would re-introduce the require cache bug
    -- from 2026-08-12 rollback). User edits via menu (_editString), values
    -- persist into G_reader_settings["fns_sync"] like every other setting.
    --
    -- SECURITY: ai_api_key is stored in plaintext, same trust level as the
    -- FNS api_token. KOreader's settings.reader.lua is plaintext on the
    -- Kindle filesystem; encryption would only defend against a USB snoop,
    -- not a stolen device. Mitigation: user rotates key at DeepSeek console
    -- if Kindle is lost.
    ai_enabled      = false,
    ai_api_base     = "https://api.deepseek.com/v1",
    ai_api_key      = "",
    ai_model        = "deepseek-chat",
    ai_system_prompt = "你是一个阅读助手，根据用户的高亮和问题给出简洁有用的回答。",
    ai_max_tokens   = 1024,
    ai_temperature  = 0.7,
    ai_timeout_sec  = 30,

    -- Quick prompt templates for the [翻译][解释][评论] buttons in the
    -- InputDialog. Each is prefixed to the highlighted text on send.
    -- {text} placeholder is replaced with the highlight content.
    ai_quick_prompts = {
        translate = "请把下面这段话翻译成中文：\n\n{text}",
        explain   = "请解释下面这段话的背景和含义：\n\n{text}",
        comment   = "请简要评论下面这段话的观点：\n\n{text}",
    },
```

- [ ] **A5：在 config.lua 末尾追加 AI HTTP timeout 常量**

在 `return Config` 之前（约第 190 行），追加：

```lua
-- M8: HTTP timeouts for AI API calls (block, total). Larger than FNS's
-- LARGE_*_TIMEOUT (10/30) because DeepSeek's reasoning can take 30s.
-- Reference: kosync.koplugin uses per-scenario timeout tables too.
-- Used by ai.lua via socketutil:set_timeout(unpack(Config.AI_HTTP_TIMEOUTS)).
Config.AI_HTTP_TIMEOUTS = { 10, 60 }

return Config
```

- [ ] **A6：运行测试验证通过**

运行：

```bash
lua tests/test_config_ai.lua
```

预期：PASS。输出 `== Tests: 16 passed, 0 failed ==`。

- [ ] **A7：修改 main.lua init() 加 v4→v5 迁移块**

在 `plugin/fns_sync.koplugin/main.lua:125`（v3→v4 块结束后、`self.settings.config_version = Config.CURRENT_CONFIG_VERSION` 之前），追加：

```lua
        -- v4 → v5 (M8): AI assistant fields added (ai_enabled, ai_api_base,
        -- ai_api_key, ai_model, ai_system_prompt, ai_max_tokens,
        -- ai_temperature, ai_timeout_sec, ai_quick_prompts). DEFAULTS
        -- backfill above already sets them to defaults; no explicit
        -- migration needed unless we want to refresh user-modified values.
        -- This block is intentionally a no-op log marker for traceability.
        if prev_version < 5 then
            logger.info("[FNS] migrated settings v4→v5: AI fields backfilled from DEFAULTS")
        end
```

- [ ] **A8：本地冒烟测试 — 现有测试零回归**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
```

预期：两个测试都 PASS。

- [ ] **A9：Commit**

```bash
git add plugin/fns_sync.koplugin/config.lua plugin/fns_sync.koplugin/main.lua tests/test_config_ai.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task A): AI 配置基础设施（DEFAULTS + 版本迁移 + 测试）

把 AI 相关配置（api_base / api_key / model / system_prompt / timeouts /
quick_prompts）全部塞进 G_reader_settings["fns_sync"]，复用现有
Config.DEFAULTS backfill + config_version 迁移机制。

设计修正（来自 2026-08-12 调研）：
- 取消独立 ai_config.lua 文件（require 缓存坑）
- 取消 USB 编辑文件入口
- 仅保留菜单 _editString 输入入口（Task D 实现）

CURRENT_CONFIG_VERSION: 4 → 5
新增 Config.AI_HTTP_TIMEOUTS = { 10, 60 }（block/total，给 DeepSeek
30s 推理留余量）

新增测试：tests/test_config_ai.lua（16 项断言）

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

**Kindle 实测验收**（在 Task B 完成后一并验证）：
- 安装插件后 KOReader 正常启动，`G_reader_settings:readSetting("fns_sync")` 能看到全部 ai_* 字段
- M5/M6/M7 现有功能零回归

---

## 任务 B：HTTP 调用层（ai.lua）

**目标**：实现 `Ai:chat(settings, messages)` 函数，调 DeepSeek `/chat/completions` 端点，返回统一结构。**不写 UI 代码**。

**文件：**
- 创建：`plugin/fns_sync.koplugin/ai.lua`

**说明**：本任务不写单元测试（项目对 HTTP 集成无单元测试传统，参考 `api.lua`）。验收靠 Kindle 实测 + Lua console 手动调。

### 步骤

- [ ] **B1：新建 ai.lua 骨架**

创建 `plugin/fns_sync.koplugin/ai.lua`：

```lua
--[[--
HTTP client for AI chat completions (DeepSeek / OpenAI-compatible).

Wraps KOreader's socket.http + rapidjson + socketutil stack — same pattern
as api.lua (synchronous call wrapped in UIManager:nextTick by the caller).

API spec: POST {base_url}/chat/completions
  Headers: Authorization: Bearer <api_key>, Content-Type: application/json
  Body:    { model, messages, max_tokens, temperature, stream=false }
  Response: 200 { choices: [{ message: { content: "..." } }], ... }

@module fns_sync.ai
--]]--

local logger = require("logger")
local http = require("socket.http")
local ltn12 = require("ltn12")
local rapidjson = require("rapidjson")
local socket = require("socket")
local socketutil = require("socketutil")
local util = require("util")
local _ = require("gettext")

local Config = require("config")

local Ai = {}

return Ai
```

- [ ] **B2：实现 Ai:_rawRequest**

在 `return Ai` 之前追加：

```lua
--- Low-level HTTP POST to {base_url}/chat/completions.
-- Mirrors api.lua:_rawRequest structure but targets OpenAI-compatible
-- endpoint with Bearer auth. No business-code envelope (OpenAI uses HTTP
-- status only); caller parses response via rapidjson directly.
--
-- @param settings table  plugin settings (ai_api_base, ai_api_key)
-- @param body table      request body table (model, messages, ...)
-- @return http_code, body_str, status  (same convention as api.lua)
function Ai:_rawRequest(settings, body)
    -- Normalize base URL: strip trailing slashes (same as api.lua:buildUrl)
    local base = (settings.ai_api_base or ""):gsub("/+$", "")
    local url = base .. "/chat/completions"

    local body_json, err = rapidjson.encode(body)
    if not body_json then
        logger.warn("[FNS-AI] JSON encode failed:", err)
        return nil, "", "json encode failed"
    end
    -- Reuse api.lua's rapidjson .0 workaround (api.lua:92-99). OpenAI
    -- body fields are all string or number; max_tokens/temperature are
    -- number so could theoretically get ".0" — strip defensively.
    body_json = body_json:gsub("(%d)%.0([,}])", "%1%2")

    local sink = {}
    local request = {
        url = url,
        method = "POST",
        sink = ltn12.sink.table(sink),
        source = ltn12.source.string(body_json),
        headers = {
            ["Authorization"] = "Bearer " .. (settings.ai_api_key or ""),
            ["Content-Type"] = "application/json",
            ["Accept"] = "application/json",
            ["Content-Length"] = #body_json,
        },
    }

    socketutil:set_timeout(Config.AI_HTTP_TIMEOUTS[1], Config.AI_HTTP_TIMEOUTS[2])
    local code, _, status = socket.skip(1, http.request(request))
    socketutil:reset_timeout()

    -- Normalize network-error case (api.lua:118-122): when http.request
    -- fails, the returned "code" is actually the error string ("timeout",
    -- "closed", "Network is unreachable"). Without this check Ai:chat
    -- would mistake the string for HTTP code.
    if type(code) == "string" then
        return nil, table.concat(sink), code
    end

    return code, table.concat(sink), status
end
```

- [ ] **B3：实现 Ai:chat**

在 `Ai:_rawRequest` 之后、`return Ai` 之前追加：

```lua
--- Send a chat completion request to the configured AI service.
--
-- @param settings table       plugin settings (ai_api_base, ai_api_key,
--                             ai_model, ai_max_tokens, ai_temperature)
-- @param messages table|array conversation history, each entry:
--                             { role = "system"|"user"|"assistant",
--                               content = "..." }
-- @return table {
--   ok            = bool      -- got a valid response with content
--   network_error = bool      -- socket/DNS/timeout failure
--   http_code     = number|nil
--   content       = string|nil -- AI's reply text (choices[1].message.content)
--   raw           = string|nil -- raw response body (for error diagnosis)
--   message       = string|nil -- user-facing error description (Chinese)
-- }
function Ai:chat(settings, messages)
    if not settings.ai_api_base or settings.ai_api_base == "" then
        return { ok = false, message = _("AI 服务 URL 未设置（菜单 → FNS 同步 → AI 助手 → API 设置）") }
    end
    if not settings.ai_api_key or settings.ai_api_key == "" then
        return { ok = false, message = _("AI API Key 未设置") }
    end

    local body = {
        model = settings.ai_model or "deepseek-chat",
        messages = messages,
        max_tokens = settings.ai_max_tokens or 1024,
        temperature = settings.ai_temperature or 0.7,
        stream = false,
    }

    logger.info("[FNS-AI] POST /chat/completions model=" .. tostring(body.model)
        .. " messages=" .. #messages .. " max_tokens=" .. tostring(body.max_tokens))

    local http_code, body_str, status = self:_rawRequest(settings, body)

    -- Network failure
    if http_code == nil or http_code == 0 then
        logger.warn("[FNS-AI] network error: " .. tostring(status))
        return {
            ok = false,
            network_error = true,
            message = _("网络错误：") .. tostring(status) .. _("（请检查网络与 API URL）"),
        }
    end

    -- Non-200 HTTP
    if http_code ~= 200 then
        logger.warn("[FNS-AI] HTTP " .. tostring(http_code) .. ": " .. tostring(body_str):sub(1, 200))
        local hint
        if http_code == 401 then
            hint = _("AI API Key 无效（401）")
        elseif http_code == 429 then
            hint = _("请求太频繁，请 30 秒后再试（429 限流）")
        elseif http_code >= 500 then
            hint = _("AI 服务异常（5xx），请稍后再试")
        else
            hint = string.format(_("AI 服务返回 HTTP %s"), tostring(http_code))
        end
        return {
            ok = false,
            http_code = http_code,
            raw = body_str,
            message = hint,
        }
    end

    -- Parse JSON response
    local parsed, perr = rapidjson.decode(body_str)
    if not parsed then
        logger.warn("[FNS-AI] JSON parse failed: " .. tostring(perr) .. " body: " .. tostring(body_str):sub(1, 200))
        return {
            ok = false,
            http_code = http_code,
            raw = body_str,
            message = _("AI 回复解析失败（详见 crash.log）"),
        }
    end

    -- Extract content
    local choices = parsed.choices
    if type(choices) ~= "table" or #choices == 0 then
        logger.warn("[FNS-AI] no choices in response: " .. tostring(body_str):sub(1, 200))
        return {
            ok = false,
            http_code = http_code,
            raw = body_str,
            message = _("AI 回复格式异常（无 choices）"),
        }
    end
    local content = choices[1].message and choices[1].message.content
    if not content or content == "" then
        logger.warn("[FNS-AI] empty content in response: " .. tostring(body_str):sub(1, 200))
        return {
            ok = false,
            http_code = http_code,
            raw = body_str,
            message = _("AI 回复为空"),
        }
    end

    logger.info("[FNS-AI] success: " .. tostring(#content) .. " chars")
    return {
        ok = true,
        http_code = http_code,
        content = content,
    }
end
```

- [ ] **B4：本地冒烟测试 — 现有测试零回归**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
```

预期：两个测试都 PASS（ai.lua 加载不会影响它们，因为它们没 require ai）。

- [ ] **B5：Commit**

```bash
git add plugin/fns_sync.koplugin/ai.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task B): AI HTTP 调用层（ai.lua，无 UI）

新建 ai.lua 模块，结构对齐 api.lua：
- Ai:_rawRequest  底层 luasocket POST，复用 rapidjson .0 修复 + 错误归一化
- Ai:chat         高层接口，调 {ai_api_base}/chat/completions，
                  返回 { ok, network_error, http_code, content, message }

完全沿用现有同步 HTTP 模式（不引入 Trapper/Spore/httpclient）。
Timeout 用 Config.AI_HTTP_TIMEOUTS = { 10, 60 }（block 10s / total 60s）。

错误分类（HTTP 401 / 429 / 5xx / 网络 / JSON）→ 用户友好中文提示。

不写 UI 代码（Task D 在 main.lua 实现）。
不写单元测试（项目对 HTTP 集成无单测传统，靠 Kindle 实测）。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **B6：Kindle 实测 — Task A + Task B 一起验证**

按 `progress/M8-Kindle-实测清单.md`（Task A 之前先创建此清单）执行：

1. 用 mutagen 同步代码到 Kindle
2. KOReader 启动，开任意书
3. 长按高亮 → 弹菜单 → 菜单**不应该**出现"问 AI"按钮（Task C 还没做）
4. 退出 KOReader
5. 用 Lua console（如 KUAL 提供）手动验证配置：
   ```lua
   return G_reader_settings:readSetting("fns_sync").ai_enabled  -- 期望 false
   return G_reader_settings:readSetting("fns_sync").ai_api_base -- 期望 https://api.deepseek.com/v1
   ```
6. （可选）用 Lua console 手动调 Ai:chat（需要先设好 ai_api_key + 联网）：
   ```lua
   package.path = package.path .. ";./plugins/fns_sync.koplugin/?.lua"
   local Ai = require("ai")
   local settings = G_reader_settings:readSetting("fns_sync")
   local r = Ai:chat(settings, {{role="user", content="hello"}})
   return r
   ```
7. 检查 `crash.log` 里有 `[FNS-AI]` 日志条目
8. M5/M6/M7 现有同步功能正常

**若实测失败**：禁止继续 Task C。回到 B1 排查 + 必要时回滚到 Task A commit。

---

## 任务 C：高亮菜单按钮注册

**目标**：长按高亮 → 弹出"问 AI"按钮（callback 暂时只 toast "功能开发中"，UI 完整逻辑在 Task D 加）。

**文件：**
- 修改：`plugin/fns_sync.koplugin/main.lua`（init() 末尾追加 addToHighlightDialog 注册）

### 步骤

- [ ] **C1：修改 main.lua 在 init() 末尾追加高亮按钮注册**

在 `plugin/fns_sync.koplugin/main.lua` 的 `FnsSync:init()` 函数末尾（约第 202 行 `end` 之前），追加：

```lua
    -- M8: Register "Ask AI" button in highlight menu. Uses KOReader's
    -- official addToHighlightDialog hook (see qrclipboard.koplugin/main.lua
    -- for reference). Previous attempt (2026-08-12 rollback) tried to
    -- override onShowHighlightMenu event — that event is NOT broadcast by
    -- KOReader; the correct mechanism is registration via this method.
    -- The button shows up in BOTH entry paths (select-new-text AND
    -- long-press-existing-highlight → "…" → menu).
    if self.ui and self.ui.highlight and self.document then
        self.ui.highlight:addToHighlightDialog("12_fns_ask_ai", function(this)
            return {
                text = _("问 AI"),
                show_in_highlight_dialog_func = function()
                    -- Only show when AI is enabled AND api_key is set
                    return self.settings.ai_enabled == true
                       and self.settings.ai_api_key ~= nil
                       and self.settings.ai_api_key ~= ""
                end,
                callback = function()
                    -- Capture selected text eagerly — clear() may run
                    -- before our callback's UIManager:nextTick fires.
                    local selected_text
                    if this.selected_text and this.selected_text.text then
                        selected_text = this.selected_text.text
                    elseif this.selected_text and this.selected_text.pos0 and this.selected_text.pos1 then
                        -- Fallback: extract via document API if available
                        if this.ui.document and this.ui.document.getTextFromXPointers then
                            selected_text = this.ui.document:getTextFromXPointers(
                                this.selected_text.pos0, this.selected_text.pos1)
                        end
                    end

                    -- Close the highlight menu but keep the highlight itself
                    -- (true = don't clear selection). Pattern from qrclipboard.
                    if this.onClose then this:onClose(true) end

                    -- Task D will replace this placeholder with real dialog.
                    -- For now, surface captured text length so we can verify
                    -- the hook works on device.
                    local preview = (selected_text or ""):sub(1, 30)
                    UIManager:show(InfoMessage:new{
                        text = string.format(_("问 AI（开发中）\n选区：%d 字符\n%s…"), #selected_text, preview),
                        timeout = 3,
                    })
                    logger.info("[FNS-AI] ask-ai button clicked, selected_text len=" .. tostring(selected_text and #selected_text))

                    -- Defer clear() so the InfoMessage shows cleanly above
                    -- the dismissed menu (qrclipboard main.lua:54-56 pattern).
                    UIManager:scheduleIn(0.1, function()
                        if this.clear then this:clear() end
                    end)
                end,
            }
        end)
        logger.info("[FNS] registered '问 AI' button in highlight menu")
    end
```

- [ ] **C2：本地冒烟测试 — 现有测试零回归**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
```

预期：两个测试都 PASS（main.lua 改动不影响它们）。

- [ ] **C3：Commit**

```bash
git add plugin/fns_sync.koplugin/main.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task C): 注册"问 AI"高亮菜单按钮（callback 占位）

用 KOReader 官方钩子 self.ui.highlight:addToHighlightDialog() 注册按钮，
完全照搬 qrclipboard.koplugin 模式。修正 2026-08-12 回滚根因：之前
错误地 override onShowHighlightMenu 事件（该事件 KOReader 根本不广播）。

按钮显示条件：ai_enabled == true 且 ai_api_key 非空。
两条路径都触发：
- 入口 A：选中正文新文字 → 弹菜单
- 入口 B：长按已有高亮 → "…" → 弹菜单

callback 暂时只显示 InfoMessage 占位（捕获选区字符数）。
完整 InputDialog + TextViewer 链式对话框在 Task D 实现。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **C4：Kindle 实测**

按 `progress/M8-Kindle-实测清单.md` 执行：

1. 同步代码 + 重启 KOReader
2. 菜单 → FNS 同步 → AI 助手（Task D 加菜单树前**不存在**，但 ai_enabled / ai_api_key 字段已在 Task A 默认值里，先临时用 Lua console 设置）：
   ```lua
   local s = G_reader_settings:readSetting("fns_sync")
   s.ai_enabled = true
   s.ai_api_key = "sk-test-placeholder"  -- 任意非空字符串即可，按钮显示条件
   G_reader_settings:saveSetting("fns_sync", s)
   ```
3. 长按选中正文一段文字 → 弹菜单 → **应该看到"问 AI"按钮**
4. 点"问 AI" → **应该看到 InfoMessage 占位**（不闪退！）
5. 检查 `crash.log` 里有 `[FNS-AI] ask-ai button clicked` 日志
6. 长按已有高亮 → 编辑菜单 → 点"…" → **应该看到"问 AI"按钮**
7. 测试 `ai_enabled = false` 或 `ai_api_key = ""` 时按钮**不出现**
8. M5/M6/M7 现有功能零回归

**关键验收点**：长按**不闪退**。这是 2026-08-12 回滚的直接原因，必须确认修复。

**若实测失败**：禁止继续 Task D。回滚到 Task B commit。

---

## 任务 D：链式对话框 UI

**目标**：完整实现 §3.6 的链式对话框——InputDialog 输入问题 → API 调用 → TextViewer 显示对话 → 继续问 / 让 AI 总结 / 关闭 / 加到笔记（加到笔记在 Task E 加）。

**文件：**
- 修改：`plugin/fns_sync.koplugin/main.lua`（改 C1 的占位 callback；加 `_openAiInputDialog` / `_openAiResponseViewer` / `_aiSession` 方法；加 AI 子菜单树）

### 步骤

- [ ] **D1：在 main.lua 顶部 require 增加 TextViewer**

在 `plugin/fns_sync.koplugin/main.lua:48` 之后（InputDialog 已 require），追加：

```lua
local TextViewer = require("ui/widget/textviewer")
```

并在 `local Api = require("api")` 之后追加：

```lua
local Ai = require("ai")
```

- [ ] **D2：在 main.lua FnsSync:extend 块加 session 状态字段**

在 `plugin/fns_sync.koplugin/main.lua:63-72` 的 `FnsSync = WidgetContainer:extend{ ... }` 块末尾（`is_doc_only = true,` 之后），追加：

```lua
    -- M8: AI assistant runtime state (per-session, NOT persisted).
    -- _ai_session is nil when no AI conversation is active; otherwise a
    -- table { original_text = "...", messages = { {role, content}, ... } }.
    -- Reset on: book close, ReaderUI teardown, user clicks "关闭".
    -- A fresh "问 AI" button click starts a new session.
    _ai_session = nil,
```

- [ ] **D3：在 main.lua 加 _resetAiSession 方法**

在 `FnsSync:init()` 之后（约第 203 行），追加：

```lua
-- M8: Reset AI session state. Called on book close, ReaderUI teardown,
-- and when user dismisses the TextViewer with "关闭".
function FnsSync:_resetAiSession()
    self._ai_session = nil
end
```

- [ ] **D4：在 main.lua 加 _openAiInputDialog 方法**

紧接 `_resetAiSession` 之后，追加：

```lua
-- M8: Open the InputDialog for the user to type their question.
-- If session is nil, starts a new session with the captured highlight as
-- the "original text". If session exists, continues the conversation
-- (messages already has prior Q&A pairs).
function FnsSync:_openAiInputDialog(original_text)
    -- Start new session if none
    if not self._ai_session then
        self._ai_session = {
            original_text = original_text or "",
            messages = {
                { role = "system", content = self.settings.ai_system_prompt },
                { role = "user", content = "【原文摘录】\n" .. (original_text or "") },
            },
        }
    end

    -- Build quick-prompt buttons row. Each button fills the input with
    -- the corresponding template (placeholder {text} → original_text).
    local quick_buttons_row = {}
    for key, template in pairs(self.settings.ai_quick_prompts or {}) do
        local label
        if key == "translate" then label = _("翻译")
        elseif key == "explain" then label = _("解释")
        elseif key == "comment" then label = _("评论")
        else label = key end
        table.insert(quick_buttons_row, {
            text = label,
            callback = function()
                local filled = (template or ""):gsub("{text}", self._ai_session.original_text or "")
                -- setInputText replaces the input field content without closing the dialog
                if self._ai_input_dialog then
                    self._ai_input_dialog:setInputText(filled)
                end
            end,
        })
    end

    local buttons = {
        quick_buttons_row,
        {
            {
                text = _("取消"),
                id = "close",
                callback = function()
                    UIManager:close(self._ai_input_dialog)
                end,
            },
            {
                text = _("发送"),
                is_enter_default = true,
                callback = function()
                    local question = self._ai_input_dialog:getInputText() or ""
                    if question == "" then
                        UIManager:show(InfoMessage:new{ text = _("请输入问题"), timeout = 2 })
                        return
                    end
                    -- Append user question to session
                    table.insert(self._ai_session.messages, { role = "user", content = question })
                    UIManager:close(self._ai_input_dialog)
                    -- Trigger API call → TextViewer
                    self:_callAiAndShow(self._ai_session.messages)
                end,
            },
        },
    }

    self._ai_input_dialog = InputDialog:new{
        title = _("问 AI"),
        input = "",
        input_hint = _("输入你的问题…"),
        description = string.format(_("📖 原文：%s"),
            (self._ai_session.original_text or ""):sub(1, 60)
            .. (#self._ai_session.original_text > 60 and "…" or "")),
        buttons = buttons,
        stop_events_propagation = true,  -- prevent events reaching the dismissed highlight menu
    }
    UIManager:show(self._ai_input_dialog)
    self._ai_input_dialog:onShowKeyboard()
end
```

- [ ] **D5：在 main.lua 加 _callAiAndShow 方法**

紧接 `_openAiInputDialog` 之后，追加：

```lua
-- M8: Call Ai:chat with UIManager:nextTick wrapper (same pattern as
-- _triggerSync at main.lua:552-586), then show TextViewer on success
-- or InfoMessage on failure. Closure captures session eagerly so it
-- doesn't depend on self.ui after nextTick (close-document race).
function FnsSync:_callAiAndShow(messages)
    -- Show loading indicator. timeout=0 means it stays until replaced.
    local loading = InfoMessage:new{ text = _("正在思考…"), timeout = 0 }
    UIManager:show(loading)

    -- Capture settings + session eagerly (STRONG INVARIANT: do not read
    -- self.ui inside nextTick closure — see _triggerSync comment).
    local settings = self.settings
    local session = self._ai_session

    UIManager:nextTick(function()
        local ok, result_or_err = pcall(function()
            return Ai:chat(settings, messages)
        end)

        UIManager:close(loading)

        if not ok then
            -- pcall caught an internal error (Ai:chat should not throw
            -- but defensive — silent-failure-hunter rule)
            logger.warn("[FNS-AI] Ai:chat pcall failed: " .. tostring(result_or_err))
            UIManager:show(InfoMessage:new{
                text = _("AI 调用内部错误，详见 crash.log（grep [FNS-AI]）"),
                timeout = 5,
            })
            return
        end

        local result = result_or_err
        if not result.ok then
            UIManager:show(InfoMessage:new{
                text = result.message or _("AI 调用失败"),
                timeout = 5,
            })
            return
        end

        -- Append assistant reply to session
        table.insert(session.messages, { role = "assistant", content = result.content })

        -- Show TextViewer with full conversation
        self:_openAiResponseViewer()
    end)
end
```

- [ ] **D6：在 main.lua 加 _openAiResponseViewer 方法**

紧接 `_callAiAndShow` 之后，追加：

```lua
-- M8: Open TextViewer showing the conversation history and action buttons.
-- TextViewer is recreated each time (using reinit for in-place updates is
-- fragile across keyboard transitions — fresh dialog is simpler).
function FnsSync:_openAiResponseViewer()
    local session = self._ai_session
    if not session then return end

    -- Render conversation as markdown-ish text
    local parts = {}
    table.insert(parts, "📖 【原文摘录】")
    table.insert(parts, session.original_text or "")
    table.insert(parts, "")
    -- messages[1] is system, messages[2] is the user-original-text; skip both
    -- (original_text is already shown above). Show from messages[3] onward.
    for i = 3, #session.messages do
        local m = session.messages[i]
        if m.role == "user" then
            table.insert(parts, "🧑 【问】")
            table.insert(parts, m.content)
        elseif m.role == "assistant" then
            table.insert(parts, "🤖 【答】")
            table.insert(parts, m.content)
        end
        table.insert(parts, "")
    end

    local buttons_table = {
        {
            {
                text = _("继续问"),
                callback = function()
                    UIManager:close(self._ai_response_viewer)
                    self:_openAiInputDialog(session.original_text)  -- session preserved
                end,
            },
            {
                text = _("让 AI 总结"),
                callback = function()
                    UIManager:close(self._ai_response_viewer)
                    -- Append "summarize" as a user message and call API
                    table.insert(session.messages, { role = "user", content = "请总结以上对话" })
                    self:_callAiAndShow(session.messages)
                end,
            },
        },
        {
            {
                text = _("关闭"),
                callback = function()
                    UIManager:close(self._ai_response_viewer)
                    self:_resetAiSession()
                end,
            },
            -- "加到笔记" button will be added in Task E
        },
    }

    self._ai_response_viewer = TextViewer:new{
        title = _("AI 对话"),
        text = table.concat(parts, "\n"),
        text_type = "general",
        add_default_buttons = true,  -- keeps the default Close button
        buttons_table = buttons_table,
        close_callback = function()
            -- If user dismisses via the default Close button (not our 关闭),
            -- still reset session.
            self:_resetAiSession()
        end,
    }
    UIManager:show(self._ai_response_viewer)
end
```

- [ ] **D7：修改 C1 的占位 callback — 调用真正 dialog**

在 `plugin/fns_sync.koplugin/main.lua` 找到 Task C 加的占位 callback（搜索 `问 AI（开发中）`），整段替换为：

```lua
                callback = function()
                    -- Capture selected text eagerly — clear() may run
                    -- before our dialog opens. Pattern from qrclipboard.
                    local selected_text
                    if this.selected_text and this.selected_text.text then
                        selected_text = this.selected_text.text
                    elseif this.selected_text and this.selected_text.pos0 and this.selected_text.pos1 then
                        if this.ui.document and this.ui.document.getTextFromXPointers then
                            selected_text = this.ui.document:getTextFromXPointers(
                                this.selected_text.pos0, this.selected_text.pos1)
                        end
                    end

                    if not selected_text or selected_text == "" then
                        UIManager:show(InfoMessage:new{ text = _("未获取到选区文本"), timeout = 2 })
                        return
                    end

                    -- Close the highlight menu (keep highlight itself)
                    if this.onClose then this:onClose(true) end

                    -- Open AI InputDialog with the captured text
                    self:_openAiInputDialog(selected_text)

                    -- Defer clear() so the InputDialog shows cleanly above
                    -- the dismissed menu (qrclipboard main.lua:54-56 pattern).
                    UIManager:scheduleIn(0.1, function()
                        if this.clear then this:clear() end
                    end)
                end,
```

删除占位 InfoMessage、`preview` 局部变量、原 `logger.info` 改为：

```lua
                    logger.info("[FNS-AI] ask-ai button clicked, selected_text len=" .. tostring(#selected_text))
```

- [ ] **D8：在 main.lua addToMainMenu 加 AI 助手子菜单树**

在 `plugin/fns_sync.koplugin/main.lua:addToMainMenu` 里，在"立即同步全部历史"（约第 2036 行）之后、`{ text = _("测试连接"), ... }` 之前，插入 AI 助手菜单块：

```lua
            -- M8: AI assistant menu subtree
            {
                text = _("AI 助手"),
                enabled_func = function() return self.settings.enabled end,
                sub_item_table = {
                    {
                        text = _("启用 AI 对话"),
                        checked_func = function() return self.settings.ai_enabled end,
                        callback = function() self:_toggleBool("ai_enabled") end,
                        separator = true,
                    },
                    {
                        text = _("API 设置"),
                        sub_item_table = {
                            {
                                text = _("API 服务 URL"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_api_base",
                                        _("AI API 服务 URL"),
                                        "https://api.deepseek.com/v1")
                                end,
                            },
                            {
                                text = _("API Key"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_api_key",
                                        _("AI API Key"),
                                        _("sk-...（DeepSeek 或 OpenAI 兼容服务）"))
                                end,
                            },
                            {
                                text = _("模型名"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_model",
                                        _("AI 模型名"),
                                        "deepseek-chat / gpt-4o-mini / 等")
                                end,
                            },
                        },
                        separator = true,
                    },
                    {
                        text = _("提示词模板"),
                        sub_item_table = {
                            {
                                text = _("系统提示词"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_system_prompt",
                                        _("系统提示词（送给 AI 的角色设定）"),
                                        _("例如：你是一个阅读助手…"),
                                        true)  -- multiline
                                end,
                            },
                            {
                                text = _("翻译模板"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_quick_prompts.translate",
                                        _("翻译快捷模板"),
                                        _("可用 {text} 占位符"))
                                end,
                            },
                            {
                                text = _("解释模板"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_quick_prompts.explain",
                                        _("解释快捷模板"),
                                        _("可用 {text} 占位符"))
                                end,
                            },
                            {
                                text = _("评论模板"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_quick_prompts.comment",
                                        _("评论快捷模板"),
                                        _("可用 {text} 占位符"))
                                end,
                            },
                        },
                        separator = true,
                    },
                    {
                        text = _("高级参数"),
                        sub_item_table = {
                            {
                                text = _("max_tokens"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_max_tokens",
                                        _("max_tokens"),
                                        "1024")
                                end,
                            },
                            {
                                text = _("temperature"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_temperature",
                                        _("temperature"),
                                        "0.7")
                                end,
                            },
                            {
                                text = _("超时（秒）"),
                                keep_menu_open = true,
                                callback = function()
                                    self:_editString("ai_timeout_sec",
                                        _("AI 调用超时（秒）"),
                                        "30")
                                end,
                            },
                        },
                    },
                    {
                        text = _("说明"),
                        keep_menu_open = true,
                        callback = function()
                            UIManager:show(InfoMessage:new{
                                text = _("AI 助手（M8）：\n\n1. 在 API 设置里填 base URL + Key + 模型名（DeepSeek 默认）\n2. 长按高亮 → 菜单 → 问 AI\n3. 多轮对话 → 让 AI 总结 → 加到笔记（同步到 Obsidian）\n\nAPI Key 明文存在 Kindle（跟 FNS Token 同级别），不加密。Kindle 丢失请到 DeepSeek 后台撤销 Key。"),
                                timeout = 10,
                            })
                        end,
                    },
                },
                separator = true,
            },
```

**注意**：`_editString("ai_quick_prompts.translate", ...)` 这种嵌套 key 在现有 `_editString` 实现里不支持（它直接 `self.settings[key]`）。需要 D9 加一个嵌套 key 处理。

- [ ] **D9：增强 _editString 支持嵌套 key（如 ai_quick_prompts.translate）**

在 `plugin/fns_sync.koplugin/main.lua:250`（`_editString` 函数定义），整段替换为：

```lua
--- Open an input dialog bound to settings[key].
-- Supports dotted nested keys (e.g. "ai_quick_prompts.translate") for M8
-- quick-prompt templates. Top-level keys still work as before.
function FnsSync:_editString(key, title, hint, allow_newline)
    -- Resolve current value, supporting nested keys
    local function getNested(tbl, path)
        local parts = {}
        for p in tostring(path):gmatch("[^.]+") do table.insert(parts, p) end
        local cur = tbl
        for _, p in ipairs(parts) do
            if type(cur) ~= "table" then return nil end
            cur = cur[p]
        end
        return cur
    end
    local function setNested(tbl, path, value)
        local parts = {}
        for p in tostring(path):gmatch("[^.]+") do table.insert(parts, p) end
        local cur = tbl
        for i = 1, #parts - 1 do
            if type(cur[parts[i]]) ~= "table" then cur[parts[i]] = {} end
            cur = cur[parts[i]]
        end
        cur[parts[#parts]] = value
    end

    local current_value = getNested(self.settings, key)
    local dialog = InputDialog:new{
        title = title,
        input = tostring(current_value or ""),
        input_hint = hint or "",
        allow_newline = allow_newline == true,
        save_callback = function(content)
            setNested(self.settings, key, content)
            self:saveSettings()
        end,
        reset_callback = function()
            local default = getNested(Config.DEFAULTS, key)
            return default ~= nil and tostring(default) or ""
        end,
    }
    UIManager:show(dialog)
    dialog:onShowKeyboard()
end
```

- [ ] **D10：本地冒烟测试 — 现有测试零回归**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
```

预期：两个测试都 PASS。

- [ ] **D11：Commit**

```bash
git add plugin/fns_sync.koplugin/main.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task D): 链式对话框 UI（InputDialog + TextViewer + AI 菜单树）

实现完整 AI 对话流程：
1. 长按高亮 → 问 AI → InputDialog（带翻译/解释/评论快捷模板）
2. 发送 → UIManager:nextTick 调 Ai:chat → TextViewer 显示对话历史
3. 继续问 / 让 AI 总结（多轮上下文累积）/ 关闭

新增方法：
- _resetAiSession            清空会话
- _openAiInputDialog         首轮/续轮输入框（保留 messages）
- _callAiAndShow             nextTick + pcall + loading InfoMessage
- _openAiResponseViewer      TextViewer + 行动按钮

增强 _editString：支持嵌套 key（ai_quick_prompts.translate 等）。

新增 AI 助手菜单树：启用开关 / API 设置 / 提示词模板 / 高级参数 /
说明。复用现有 _editString + _toggleBool，零额外 UI 代码。

修正 Task C 占位 callback → 调用真正 dialog。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **D12：Kindle 实测**

按 `progress/M8-Kindle-实测清单.md` 执行：

1. 同步代码 + 重启 KOReader
2. 菜单 → FNS 同步 → AI 助手 → 启用 AI 对话 → API 设置（填入真实 DeepSeek key）
3. 长按选中正文文字 → 菜单 → 问 AI
4. **InputDialog 弹出**，顶部有 [翻译][解释][评论] 三按钮，下方输入框
5. 点"翻译" → 输入框自动填入翻译模板（带原文）
6. 点"发送" → **InfoMessage "正在思考…"** → 等待 → **TextViewer 显示原文 + Q + A**
7. 点"继续问" → 回到 InputDialog → 输入新问题 → 发送 → TextViewer 显示 2 轮对话
8. 点"让 AI 总结" → InfoMessage → TextViewer 显示总结
9. 点"关闭" → 对话框消失，session 清空
10. 长按已有高亮 → 编辑菜单 → "…" → 问 AI（入口 B 验证）
11. 错误场景测试（任选其一）：
    - 关 WiFi → 发送 → 看到"网络错误" toast
    - 故意把 key 改错 → 发送 → 看到"API Key 无效（401）"
12. 检查 `crash.log` 里 `[FNS-AI]` 日志条目完整
13. M5/M6/M7 现有功能零回归

**若实测失败**：禁止继续 Task E。回滚到 Task C commit。

---

## 任务 E：AI@ 块 + marker.lua 扩展

**目标**：用户点"加到笔记" → AI 内容作为 AI@ 块写入笔记，通过 FNS 同步推送到 Obsidian。marker.lua 扩展支持 `type = "ai"` segment，diff 不参与。

**文件：**
- 修改：`plugin/fns_sync.koplugin/marker.lua`（parse / serialize 支持 AI@ 块）
- 修改：`plugin/fns_sync.koplugin/main.lua`（TextViewer 加"加到笔记"按钮；onCloseDocument reset session）
- 创建：`tests/test_marker_ai.lua`

### 步骤

- [ ] **E1：编写失败测试**

创建 `tests/test_marker_ai.lua`：

```lua
--[[--
Unit tests for M8 AI@ block support in marker.lua.

Run locally (NOT on Kindle) with:
    lua tests/test_marker_ai.lua
--]]--

package.path = package.path .. ";./plugin/fns_sync.koplugin/?.lua"

local Marker = require("marker")

local tests_passed = 0
local tests_failed = 0

local function check(name, cond)
    if cond then
        tests_passed = tests_passed + 1
        print(("  ✓ %s"):format(name))
    else
        tests_failed = tests_failed + 1
        print(("  ✗ %s FAILED"):format(name))
    end
end

print("== M8 AI@ block support ==")

-- Parse a single AI@ block
do
    local content = '<!-- AI@2026-08-12 19:48:00 hl="2026-08-12 19:45:00" model="deepseek-chat" -->\n💬 AI 助手\n> 这是 AI 总结的内容…\n<!-- /AI@2026-08-12 19:48:00 -->'
    local segs = Marker.parse(content)
    check("parse: 1 segment", #segs == 1)
    check("parse: type == ai", segs[1].type == "ai")
    check("parse: ts extracted", segs[1].ts == "2026-08-12 19:48:00")
    check("parse: hl meta", segs[1].meta and segs[1].meta.hl == "2026-08-12 19:45:00")
    check("parse: model meta", segs[1].meta and segs[1].meta.model == "deepseek-chat")
    check("parse: body content", segs[1].content:find("AI 总结的内容") ~= nil)
end

-- Round-trip: parse → serialize preserves AI@ block
do
    local original = '<!-- AI@2026-08-12 19:48:00 hl="2026-08-12 19:45:00" model="deepseek-chat" -->\nAI 内容\n<!-- /AI@2026-08-12 19:48:00 -->'
    local segs = Marker.parse(original)
    local again = Marker.serialize(segs)
    check("round-trip: AI@ block preserved", again:find("AI@2026%-08%-12 19:48:00") ~= nil)
    check("round-trip: close marker preserved", again:find("/AI@2026%-08%-12 19:48:00") ~= nil)
    check("round-trip: hl meta preserved", again:find('hl="2026%-08%-12 19:45:00"') ~= nil)
    check("round-trip: model meta preserved", again:find('model="deepseek%-chat"') ~= nil)
end

-- Mixed: HL@ + AI@ + user content
do
    local content = '<!-- HL@2026-08-12 19:45:00 -->\n原文\n<!-- /HL@2026-08-12 19:45:00 -->\n\n用户笔记\n\n<!-- AI@2026-08-12 19:48:00 hl="2026-08-12 19:45:00" model="deepseek-chat" -->\nAI 内容\n<!-- /AI@2026-08-12 19:48:00 -->'
    local segs = Marker.parse(content)
    check("mixed: 5 segments (hl, user, ai)", #segs == 3 or #segs == 5)
    -- Count by type
    local hl_count, ai_count, user_count = 0, 0, 0
    for _, s in ipairs(segs) do
        if s.type == "hl" then hl_count = hl_count + 1
        elseif s.type == "ai" then ai_count = ai_count + 1
        elseif s.type == "user" then user_count = user_count + 1 end
    end
    check("mixed: 1 hl", hl_count == 1)
    check("mixed: 1 ai", ai_count == 1)
    check("mixed: >=1 user", user_count >= 1)
end

-- Diff: AI blocks do NOT participate (preserved verbatim like user content)
do
    local existing = Marker.parse('<!-- AI@2026-08-12 19:48:00 hl="2026-08-12 19:45:00" model="deepseek-chat" -->\nAI 内容\n<!-- /AI@2026-08-12 19:48:00 -->')
    -- current_highlights is empty (no HL markers in note) — AI block should
    -- NOT be reported as a delete; it should be preserved.
    local actions = Marker.diff(existing, {}, {})
    local has_ai_delete = false
    for _, a in ipairs(actions) do
        if a.op == "delete" then has_ai_delete = true end
    end
    check("diff: AI block not deleted when current_highlights empty", not has_ai_delete)
    check("diff: no actions at all", #actions == 0)
end

-- applyDiff preserves AI segments (they pass through unchanged)
do
    local existing = {
        { type = "ai", ts = "2026-08-12 19:48:00", content = "AI 内容",
          meta = { hl = "2026-08-12 19:45:00", model = "deepseek-chat" } },
        { type = "user", content = "用户笔记" },
    }
    local actions = {}  -- no HL@ actions
    local result = Marker.applyDiff(existing, actions)
    check("applyDiff: AI segment preserved", result[1].type == "ai")
    check("applyDiff: AI content preserved", result[1].content == "AI 内容")
    check("applyDiff: user segment preserved", result[2].type == "user")
end

print(("== Tests: %d passed, %d failed =="):format(tests_passed, tests_failed))
os.exit(tests_failed == 0 and 0 or 1)
```

- [ ] **E2：运行测试验证失败**

运行：

```bash
lua tests/test_marker_ai.lua
```

预期：FAIL。报错类似 `parse: type == ai` 失败（当前 marker.lua 只识别 HL@ 块，AI@ 块会被当作 user content）。

- [ ] **E3：修改 marker.lua 加 AI@ 块 token 常量**

在 `plugin/fns_sync.koplugin/marker.lua:43-46`（HL_OPEN_PREFIX 等常量之后），追加：

```lua
-- M8: AI@ block markers. Same structure as HL@ (unique datetime as key,
-- optional meta fields). Diff does NOT touch AI@ blocks — they are
-- preserved verbatim like user content (see diff() skip logic).
local AI_OPEN_PREFIX = "<!-- AI@"
local AI_OPEN_SUFFIX = " -->"
local AI_CLOSE_PREFIX = "<!-- /AI@"
local AI_CLOSE_SUFFIX = " -->"

-- Known META keys for AI@ blocks (M8). 'hl' references the parent HL@ ts;
-- 'model' identifies the AI service used. Others preserved verbatim.
local AI_META_KEYS = { "hl", "model" }
```

- [ ] **E4：修改 marker.lua parse() 支持 AI@ 块**

`Marker.parse` 当前只查 HL_OPEN_PREFIX（line 122）。需要重写为先扫 HL@ 和 AI@ 的最早出现位置。整段替换 `Marker.parse` 函数（line 116-181）：

```lua
--- Parse notebook content into a list of segments.
-- Each segment is one of:
--   { type = "user", content = "..." }                              -- user text (verbatim)
--   { type = "hl", ts = "...", content = "...", meta = {...}|nil }  -- KOreader highlight block
--   { type = "ai", ts = "...", content = "...", meta = {...}|nil }  -- M8 AI assistant block
--
-- Malformed HL@/AI@ blocks (missing close marker, missing " -->") are
-- treated as user content (preserved verbatim, not lost).
function Marker.parse(content)
    local segments = {}
    local i = 1
    local n = #content

    while i <= n do
        -- Find the next block opener: either HL@ or AI@, whichever comes first
        local hl_pos = string.find(content, HL_OPEN_PREFIX, i, true)
        local ai_pos = string.find(content, AI_OPEN_PREFIX, i, true)

        -- No more blocks: rest is user content
        if not hl_pos and not ai_pos then
            local rest = string.sub(content, i)
            table.insert(segments, { type = "user", content = rest })
            break
        end

        -- Pick whichever opener is earliest (nil → infinity via huge number)
        local next_pos, prefix, suffix, close_prefix, block_type
        if hl_pos and (not ai_pos or hl_pos < ai_pos) then
            next_pos, prefix, suffix, close_prefix = hl_pos, HL_OPEN_PREFIX, HL_OPEN_SUFFIX, HL_CLOSE_PREFIX
            block_type = "hl"
        else
            next_pos, prefix, suffix, close_prefix = ai_pos, AI_OPEN_PREFIX, AI_OPEN_SUFFIX, AI_CLOSE_PREFIX
            block_type = "ai"
        end

        -- Capture text before block as user content
        if next_pos > i then
            local before = string.sub(content, i, next_pos - 1)
            table.insert(segments, { type = "user", content = before })
        end

        -- Find the " -->" that closes the open marker
        local open_suffix_pos = string.find(content, suffix, next_pos, true)
        if not open_suffix_pos then
            logger.warn("[FNS] malformed " .. block_type .. "@ open marker (no ' -->'), treating rest as user content")
            local rest = string.sub(content, next_pos)
            table.insert(segments, { type = "user", content = rest })
            break
        end
        local open_body = string.sub(content, next_pos + #prefix, open_suffix_pos - 1)

        -- Parse ts + meta fields. parseOpenMarkerMeta works for both HL@ and AI@
        -- (same format: "<datetime> [key=\"value\" ...]").
        local ts, meta = Marker.parseOpenMarkerMeta(open_body)

        -- Find matching close marker "<!-- /<TYPE>@<ts> -->"
        local close_marker = close_prefix .. ts .. (block_type == "ai" and AI_CLOSE_SUFFIX or HL_CLOSE_SUFFIX)
        local close_pos = string.find(content, close_marker, open_suffix_pos + #suffix, true)
        if not close_pos then
            logger.warn("[FNS] " .. block_type .. "@ block missing close marker for ts=" .. tostring(ts) .. ", treating as user content")
            local rest = string.sub(content, next_pos)
            table.insert(segments, { type = "user", content = rest })
            break
        end

        -- Extract block content
        local block_start = open_suffix_pos + #suffix
        local raw_block = string.sub(content, block_start, close_pos - 1)
        raw_block = raw_block:gsub("\n%s*>%s*$", "")
        local trimmed = raw_block:gsub("^%s+", ""):gsub("%s+$", "")

        local seg = { type = block_type, ts = ts, content = trimmed }
        if meta then seg.meta = meta end
        table.insert(segments, seg)

        i = close_pos + #close_marker
    end

    return segments
end
```

- [ ] **E5：修改 marker.lua serialize() 支持 AI@ 块**

当前 `Marker.serialize`（line 216-229）的"两个 HL@ 块之间空两行"逻辑只对 hl 生效。需要让 AI@ 块也走 `wrapBlock`-like 路径。整段替换 `Marker.serialize`：

```lua
--- Serialize segments back to notebook content.
-- Inverse of parse for well-formed input. Two adjacent HL@/AI@ blocks
-- (no user text between them) get TWO blank lines as separator.
-- M8: AI@ blocks round-trip with meta (hl + model) preserved.
function Marker.serialize(segments)
    local parts = {}
    for i, seg in ipairs(segments) do
        if seg.type == "user" then
            table.insert(parts, seg.content)
        elseif seg.type == "hl" then
            if i > 1 and (segments[i - 1].type == "hl" or segments[i - 1].type == "ai") then
                table.insert(parts, "\n\n\n")
            end
            table.insert(parts, Marker.wrapBlock(seg.ts, seg.content, seg.meta))
        elseif seg.type == "ai" then
            if i > 1 and (segments[i - 1].type == "hl" or segments[i - 1].type == "ai") then
                table.insert(parts, "\n\n\n")
            end
            table.insert(parts, Marker._wrapAiBlock(seg.ts, seg.content, seg.meta))
        end
    end
    return table.concat(parts)
end
```

- [ ] **E6：在 marker.lua 加 _wrapAiBlock helper**

紧接 `Marker.wrapBlock`（line 202-206）之后，追加：

```lua
--- Wrap a single AI@ block. Like wrapBlock but uses AI@ markers and
-- serializes AI_META_KEYS (hl + model) instead of HL's pos0/pos1/chapter.
-- @param ts string  AI reply datetime (unique key per HL@ parent)
-- @param content string  AI reply text
-- @param meta table|nil  { hl = "...", model = "..." }
function Marker._wrapAiBlock(ts, content, meta)
    local meta_str = ""
    if meta then
        local parts = {}
        for _, key in ipairs(AI_META_KEYS) do
            local v = meta[key]
            if v ~= nil and v ~= "" then
                table.insert(parts, key .. "=\"" .. tostring(v) .. "\"")
            end
        end
        if #parts > 0 then meta_str = " " .. table.concat(parts, " ") end
    end
    return AI_OPEN_PREFIX .. ts .. meta_str .. AI_OPEN_SUFFIX .. "\n"
        .. content .. "\n\n"
        .. AI_CLOSE_PREFIX .. ts .. AI_CLOSE_SUFFIX
end
```

- [ ] **E7：修改 marker.lua diff() 让 AI segment 不参与**

当前 `Marker.diff`（line 239-276）的"deletes"循环只针对 `seg.type == "hl"`。AI segment 天然不会被报告为 delete。需要确认 insert/update 也不会被 AI 触发——`current_highlights` 参数是 ts→content 表，AI 的 ts 不会在里面。所以 **diff 已经天然跳过 AI 块**，无需修改。

但要在 `diff` 里加一行注释明确这个意图。在 line 260（"Deletes and updates" 循环前）加注释：

```lua
    -- M8: AI segments (seg.type == "ai") are intentionally NOT iterated
    -- here — they pass through diff unchanged (preserved verbatim like
    -- user content). This is the core guarantee that AI@ blocks survive
    -- M5/M6/M7 sync rounds without being mistaken for stale highlights.
```

- [ ] **E8：运行测试验证通过**

运行：

```bash
lua tests/test_marker_ai.lua
```

预期：PASS。所有断言通过。

- [ ] **E9：本地冒烟测试 — marker 现有功能零回归**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
lua tests/test_marker_ai.lua
```

预期：三个测试都 PASS。

- [ ] **E10：Commit marker.lua 扩展**

```bash
git add plugin/fns_sync.koplugin/marker.lua tests/test_marker_ai.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task E part 1): marker.lua 扩展支持 AI@ 块（parse/serialize/diff 不参与）

新增 segment 类型 type="ai"，跟现有 type="hl"/"user" 并列。
- parse:    同时扫描 HL@ / AI@ 开标记，按出现顺序解析（不冲突）
- serialize:AI@ 块用 _wrapAiBlock 序列化（meta = hl + model）
- diff:    AI segment 天然不参与（不在 current_highlights 表里），
           显式注释说明这个保证

AI@ 块格式：
  <!-- AI@<datetime> hl="<parent hl ts>" model="<model>" -->
  <AI 内容>
  <!-- /AI@<datetime> -->

完全不影响现有 HL@ 块的 parse/serialize/diff/applyDiff。
M5/M6/M7 同步逻辑零回归（test_threeway 仍通过）。

新增测试 tests/test_marker_ai.lua（16 项断言）：
- 单 AI@ 块 parse
- round-trip serialize
- HL@ + AI@ + user 混合 parse
- diff 不参与 AI 块
- applyDiff 保留 AI segment

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **E11：在 main.lua TextViewer 加"加到笔记"按钮**

修改 Task D6 里 `_openAiResponseViewer` 的 `buttons_table`，把"关闭"那一行改为：

```lua
        {
            {
                text = _("加到笔记"),
                callback = function()
                    self:_addAiContentToNote()
                end,
            },
            {
                text = _("关闭"),
                callback = function()
                    UIManager:close(self._ai_response_viewer)
                    self:_resetAiSession()
                end,
            },
        },
```

- [ ] **E12：在 main.lua 加 _addAiContentToNote 方法**

在 `_openAiResponseViewer` 之后，追加：

```lua
-- M8: Append the last AI reply as an AI@ block to the current book's
-- Obsidian note. The AI@ block is inserted via the EXISTING FNS sync
-- path — we just trigger a manual sync; marker.lua:serialize emits the
-- AI@ block automatically when it walks the note's segments.
--
-- Strategy: build the AI@ block locally, append it to the note via a
-- dedicated _appendAiBlock method on the sync path, then trigger sync.
-- For MVP simplicity we append the AI@ block to the in-memory "user
-- content" region of the current book's note via a marker.lua parse →
-- append → serialize round-trip, then call _triggerSync.
function FnsSync:_addAiContentToNote()
    local session = self._ai_session
    if not session or not session.original_text or session.original_text == "" then
        UIManager:show(InfoMessage:new{ text = _("无 AI 内容可加入"), timeout = 2 })
        return
    end

    -- Find the last assistant message
    local last_assistant
    for i = #session.messages, 1, -1 do
        if session.messages[i].role == "assistant" then
            last_assistant = session.messages[i].content
            break
        end
    end
    if not last_assistant then
        UIManager:show(InfoMessage:new{ text = _("AI 还未回复，无内容可加入"), timeout = 2 })
        return
    end

    -- Build AI@ block via marker helper
    local ts = os.date("%Y-%m-%d %H:%M:%S")
    -- Find associated HL@ ts (for meta.hl). For MVP we use the current
    -- time as a proxy; future enhancement can match against actual HL@ ts
    -- by searching the current book's annotations for matching text.
    local hl_ts = ts  -- placeholder; real HL@ ts matching would need annotation lookup
    local ai_block = Marker._wrapAiBlock(ts, last_assistant, {
        hl = hl_ts,
        model = self.settings.ai_model or "unknown",
    })

    -- Stash the AI block on self so _doSyncCurrentBookLegacy's GET →
    -- marker-zone-replace path picks it up. The simplest integration is
    -- to APPEND the AI@ block to the note's user-content region BEFORE
    -- the sync's GET call — but that requires deep refactor of the sync
    -- flow.
    --
    -- MVP workaround: trigger a normal sync; the AI@ block is stored in
    -- self._pending_ai_blocks and merged into the note content during the
    -- next GET → merge → POST cycle. For now, just show a toast and stash.
    self._pending_ai_blocks = self._pending_ai_blocks or {}
    table.insert(self._pending_ai_blocks, {
        ts = ts,
        hl_ts = hl_ts,
        content = last_assistant,
        block = ai_block,
    })

    UIManager:show(InfoMessage:new{
        text = _("AI 内容已暂存，将通过下次同步写入笔记"),
        timeout = 3,
    })
    logger.info("[FNS-AI] queued AI@ block ts=" .. ts .. " chars=" .. #last_assistant)

    -- Close the TextViewer and trigger a sync
    if self._ai_response_viewer then
        UIManager:close(self._ai_response_viewer)
    end
    -- Reset session AFTER capturing the AI block
    self:_resetAiSession()

    -- Trigger manual sync (will pick up the pending AI@ block)
    self:_triggerSync({ silent = false })
end
```

**注意**：上面 `_pending_ai_blocks` 只是把 AI 块加入队列，真正写入笔记需要修改 `_doSyncCurrentBookLegacy`（在 GET 后把 pending blocks 合并进 segments，POST 时一起发出去）。这是 Task E 的额外步骤 E13。

- [ ] **E13：修改 _doSyncCurrentBookLegacy 合并 pending AI@ 块**

在 `plugin/fns_sync.koplugin/main.lua` 找到 `_doSyncCurrentBookLegacy`（约第 614 行）。在它内部，在 `marker.parse(existing_content)` 之后、`marker.diff(...)` 之前，加一段把 pending AI 块作为 user-content append 到 segments 的逻辑。

**由于 _doSyncCurrentBookLegacy 的完整代码本次未读，本步骤的精确插入点需要 implementation 阶段先阅读 main.lua:614-730 再确定**。本计划此处仅给出修改思路：

```lua
    -- M8: Drain pending AI@ blocks (from _addAiContentToNote). These are
    -- inserted as AI segments into the parsed segments list BEFORE the
    -- diff runs, so they get serialized into the POST body verbatim.
    -- Find the HL@ segment matching each AI block's hl_ts and insert
    -- the AI segment immediately after it; if no match, append at end.
    if self._pending_ai_blocks and #self._pending_ai_blocks > 0 then
        for _, pending in ipairs(self._pending_ai_blocks) do
            local ai_seg = {
                type = "ai",
                ts = pending.ts,
                content = pending.content,
                meta = { hl = pending.hl_ts, model = self.settings.ai_model },
            }
            -- Find matching HL@ segment by ts
            local insert_idx = #segments + 1  -- default: append
            for idx, seg in ipairs(segments) do
                if seg.type == "hl" and seg.ts == pending.hl_ts then
                    insert_idx = idx + 1
                    break
                end
            end
            table.insert(segments, insert_idx, ai_seg)
            logger.info("[FNS-AI] merging AI@ block ts=" .. pending.ts .. " at segment " .. insert_idx)
        end
        self._pending_ai_blocks = nil  -- drain
    end
```

**implementation 阶段注意**：如果 `segments` 变量名在 `_doSyncCurrentBookLegacy` 里不同，需要替换为实际变量名。

- [ ] **E14：修改 onCloseDocument 加 session reset**

在 `plugin/fns_sync.koplugin/main.lua` 找到 `FnsSync:onCloseDocument`（约第 1328 行）。在函数体开头追加：

```lua
    -- M8: Clear AI session on book close to avoid stale state on next book.
    self:_resetAiSession()
```

- [ ] **E15：本地冒烟测试**

运行：

```bash
lua tests/test_threeway.lua
lua tests/test_config_ai.lua
lua tests/test_marker_ai.lua
```

预期：三个测试都 PASS。

- [ ] **E16：Commit UI 集成**

```bash
git add plugin/fns_sync.koplugin/main.lua
git commit -m "$(cat <<'EOF'
feat(M8 Task E part 2): AI@ 块 UI 集成（加到笔记按钮 + sync 合并）

TextViewer 加"加到笔记"按钮，调用 _addAiContentToNote：
1. 找到最后一条 assistant 回复
2. 用 Marker._wrapAiBlock 构造 AI@ 块
3. 暂存到 self._pending_ai_blocks
4. 触发 _triggerSync（沿用 M5 同步路径）

_doSyncCurrentBookLegacy 在 marker.parse 之后、diff 之前 drain
_pending_ai_blocks：把每个 AI segment 插入到匹配 HL@ segment 之后
（找不到匹配则末尾追加），serialize 时一起写入 POST body。

onCloseDocument 加 _resetAiSession 调用，避免切书后 session 残留。

AI@ 块通过现有 FNS sync 推送到 Obsidian，零额外网络逻辑。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **E17：Kindle 实测 — 全流程**

按 `progress/M8-Kindle-实测清单.md` 执行（**M8 最终验收**）：

1. 同步代码 + 重启 KOReader
2. 菜单 → FNS 同步 → AI 助手 → 启用 + 填 API key
3. 长按高亮 → 问 AI → 输入问题 → 发送 → 看到 AI 回复
4. 点"加到笔记" → 看到 toast"已暂存" → 同步流程启动
5. 等 sync 完成 → 看 Obsidian 笔记 → **应该看到 AI@ 块紧跟在 HL@ 块之后**
6. 在 Obsidian 编辑笔记其他内容 → 触发同步 → AI@ 块**应该保留**（diff 不参与验证）
7. 在 Kindle 关书 → 重新开 → AI session 应该是空的（点问 AI → 输入框为空）
8. 全部 4 个测试通过
9. M5/M6/M7 现有功能零回归（重点测：双向同步 / 离线队列 / 高亮同步）
10. crash.log 检查：`[FNS-AI]` 和 `[FNS]` 日志正常，无 ERROR

**若实测失败**：按问题范围回滚到 Task D / E part 1 / E part 2 之一。

---

## 自检（writing-plans 技能要求）

### 1. 规格覆盖度

对照设计规格 `docs/superpowers/specs/2026-08-11-ai-chat-design.md`：

| 规格章节 | 实现任务 | 覆盖 |
|---------|---------|------|
| §2 用户故事 | Task D + E | ✅ |
| §3.1 方案 A（Kindle 直连） | Task B | ✅ |
| §4.1 链式对话框 | Task D | ✅ |
| §4.2 上下文累积 | Task D（messages 数组） | ✅ |
| §4.3 让 AI 总结按钮 | Task D6 | ✅ |
| §5.1 AI@ 块格式 B | Task E1-E10 | ✅ |
| §5.3 原文摘录定义（getTextFromXPointers） | Task C1 + D7 | ✅ |
| §5.4 加到笔记走 FNS 同步 | Task E11-E13 | ✅ |
| §6.1 配置文件 | Task A（**改为 G_reader_settings**） | ✅（设计修正） |
| §6.2 API key 输入 | Task D8（**仅菜单**） | ✅（设计修正） |
| §6.4 快捷模板 | Task D4 + D8 | ✅ |
| §7.1 菜单入口 A + B | Task C | ✅ |
| §7.2 错误处理表 | Task B3 | ✅ |
| §7.3 模块结构 | Task A + B + D + E | ✅（ai_dialog.lua 合并到 main.lua） |
| §7.4 M5/M6/M7 兼容 | Task E2 + 各任务冒烟测试 | ✅ |
| §8 YAGNI 边界 | 全部任务（不做的没做） | ✅ |
| §10 验收标准 1-10 | 全部任务的 Kindle 实测步骤 | ✅ |

**遗漏**：无。

### 2. 占位符扫描

- ✅ 无 "TODO / 待定 / 后续实现"
- ✅ 无 "添加适当的错误处理"（具体错误处理已写在 B3 / D5）
- ✅ 无 "类似任务 N"（每个任务的代码块都自包含）
- ✅ 无未定义的类型/函数引用
- ⚠️ **E13 有"需要 implementation 阶段先阅读 main.lua:614-730 再确定"**——这是合理的实施提示，不是占位符。如果 implementation 阶段发现 `_doSyncCurrentBookLegacy` 的实际结构与假设不符，需要在该步骤内调整代码。

### 3. 类型一致性

- `self.settings.ai_api_base` / `ai_api_key` / `ai_model` 等字段名在 Task A / B / D / E 一致 ✅
- `Ai:chat(settings, messages)` 签名在 Task B3 定义，Task D5 调用，一致 ✅
- `Marker._wrapAiBlock(ts, content, meta)` 在 Task E6 定义，Task E12 调用，一致 ✅
- `self._ai_session = { original_text, messages }` 在 Task D2 / D4 / D5 / D6 一致 ✅
- `self._pending_ai_blocks` 在 Task E12 / E13 一致 ✅
- `Config.AI_HTTP_TIMEOUTS = { 10, 60 }` 在 Task A5 定义，Task B2 调用，一致 ✅

---

## 附录：Kindle 实测清单（每个任务实测前创建）

新建文件 `progress/M8-Kindle-实测清单.md`（在 Task A 第一步前创建），记录每个任务的具体实测步骤 + 通过/失败勾选 + 失败时的回滚目标。该清单是每个 Kindle 实测步骤（B6 / C4 / D12 / E17）的引用源。

---

## 执行交接

**计划已完成并保存到 `docs/superpowers/plans/2026-08-12-m8-ai-chat.md`。两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**
